{
  "name": "silverpeak-design-build",
  "version": "1.0.0",
  "private": true,
  "description": "Silver Peak Design Build — static marketing site for silverpeakdesignbuild.com",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.31.0"
  }
}
